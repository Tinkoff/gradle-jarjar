apply plugin: 'groovy'
apply plugin: 'maven'

dependencies {
    compile gradleApi()
    compile localGroovy()
}

group = 'ru.tcsbank.repackage'
version = '1.0.22'

uploadArchives {
    repositories {
        mavenDeployer {
          //your repository 
        }
    }
}
